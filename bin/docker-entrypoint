#!/bin/bash
set -e

# Lösche eine alte Server-ID, falls vorhanden
if [ -f "/rails/tmp/pids/server.pid" ]; then
  rm /rails/tmp/pids/server.pid
fi

# Führe das aus, was als Command übergeben wurde (z.B. bin/dev)
exec "$@"