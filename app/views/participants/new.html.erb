<% content_for :title, "Person erfassen" %>

<div class="max-w-2xl mx-auto w-full px-4 mt-10 mb-16">
  <div class="mb-8">
    <%= link_to "← Zurück zum Profil", participants_path,
          class: "text-sm font-semibold text-gray-500 hover:text-gray-800 transition" %>
    <h1 class="font-extrabold text-2xl sm:text-3xl text-gray-900 mt-3">Person erfassen</h1>
    <p class="text-gray-500 mt-1">Erfasse eine Person, um sie für Kurse anzumelden.</p>
  </div>

  <%= render "form", participant: @participant %>
</div>
