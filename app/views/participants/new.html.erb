<div class="max-w-3xl mx-auto w-full">
  <div class="mb-6">
    <h1 class="font-bold text-3xl text-gray-800">Neuen Teilnehmer anlegen</h1>
    <p class="text-gray-500 text-sm mt-1">Trage die Daten des Kindes / Teilnehmers ein.</p>
  </div>

  <%= render "form", participant: @participant %>
</div>
