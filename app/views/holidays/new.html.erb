<% content_for :title, t('holidays.new.title') %>

<div class="max-w-2xl mx-auto mt-10 w-full mb-16">
  <div class="mb-6">
    <h1 class="font-bold text-3xl text-gray-900"><%= t('holidays.new.title') %></h1>
    <p class="text-gray-500 mt-1"><%= t('holidays.new.subtitle') %></p>
  </div>

  <div class="bg-white shadow-sm ring-1 ring-gray-200 rounded-xl p-6">
    <%= render "form", holiday: @holiday %>
  </div>
</div>
