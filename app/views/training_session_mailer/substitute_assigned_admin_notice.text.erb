Hallo <%= greeting_name(@admin_user) %>,

zur Info: für das folgende Training wurde eine Ersatztrainer:in eingetragen:

Kurs:            <%= @course.title %>
Datum:           <%= format_session_range(@training_session.start_time, @training_session.end_time) %>
Ersatztrainer:in: <%= @substitute_trainer.full_name %>
<% if @training_session.substitute_reason.present? %>Grund:           <%= @training_session.substitute_reason %>
<% end %>
Training ansehen: <%= @training_session_url %>

Diese Benachrichtigung kannst du unter "Meine Benachrichtigungen" für dich persönlich abschalten.

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
