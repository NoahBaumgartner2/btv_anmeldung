Hallo <%= greeting_name(@substitute_trainer.user) %>,

du wurdest für das folgende Training als Ersatztrainer:in eingetragen und hast Zugriff auf die Präsenzkontrolle:

Kurs:  <%= @course.title %>
Datum: <%= format_session_range(@training_session.start_time, @training_session.end_time) %>

Präsenzkontrolle öffnen: <%= @training_session_url %>

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
