Hallo,

Der Kurs "<%= @course.title %>" ist bereit für die nächste Periode (<%= @course.next_term&.name %>).
Für diesen Kurs ist "Automatisch erstellen" deaktiviert – du kannst die Verlängerung jetzt manuell auslösen.

Zur Kursverwaltung: <%= @manage_url %>

Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
