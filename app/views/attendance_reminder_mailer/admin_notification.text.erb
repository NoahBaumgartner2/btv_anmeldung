⚠️ Anwesenheit nicht erfasst

Für folgende Trainingseinheit wurde die Anwesenheit nicht erfasst (mehr als 7 Tage überfällig):

Kurs:  <%= @course.title %>
Datum: <%= I18n.l(@training_session.start_time, format: :long) %> – <%= I18n.l(@training_session.end_time, format: :short) %>

Zugewiesene Trainer:
<% if @trainer_emails.any? %>
<% @trainer_emails.each do |email| %>
- <%= email %>
<% end %>
<% else %>
  (keine Trainer zugewiesen)
<% end %>

Trainingseinheit ansehen: <%= @training_session_url %>

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
