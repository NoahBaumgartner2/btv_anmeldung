Hallo <%= greeting_name(@trainer.user) %>!

Für folgende Trainingseinheit wurde die Anwesenheit noch nicht erfasst:

Kurs:  <%= @course.title %>
Datum: <%= format_session_range(@training_session.start_time, @training_session.end_time) %>

Bitte erfasse die Anwesenheit so bald wie möglich, damit die Teilnehmer korrekt verwaltet werden können.

Anwesenheit erfassen: <%= @training_session_url %>

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
