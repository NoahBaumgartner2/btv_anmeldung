Hallo <%= greeting_name(@recipient) %>,

die Reservierung von <%= @participant.first_name %> <%= @participant.last_name %> für den Kurs
"<%= @course.title %>" wurde automatisch storniert, da die Zahlung nicht innerhalb von 48 Stunden
eingegangen ist. Der Kursplatz wurde wieder freigegeben. Wenn eine Teilnahme trotzdem noch gewünscht
ist, kann die definitive Anmeldung bei verfügbaren Plätzen selbstverständlich noch vorgenommen werden.

Teilnehmer: <%= @participant.first_name %> <%= @participant.last_name %>
Kurs:        <%= @course.title %>

Zur Kursanmeldung: <%= course_url(@course) %>

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
