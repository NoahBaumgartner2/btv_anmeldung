Hallo <%= greeting_name(@recipient) %>,

die Reservierung von <%= @participant.first_name %> <%= @participant.last_name %> für den Kurs
"<%= @course.title %>" wurde nach einer Woche nach dem Schnuppertraining automatisch storniert,
da die Kursanmeldung inkl. Bezahlung der Kurskosten nicht innerhalb von einer Woche vorgenommen
wurde. Der Kursplatz wurde wieder freigegeben. Wenn eine Teilnahme trotzdem noch gewünscht ist,
kann die definitive Anmeldung bei verfügbaren Plätzen selbstverständlich noch vorgenommen werden.

Teilnehmer: <%= @participant.first_name %> <%= @participant.last_name %>
Kurs:        <%= @course.title %>

Zur Kursanmeldung: <%= course_url(@course) %>

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
