Rückerstattung prüfen

Ein Trainer hat <%= @participant.first_name %> <%= @participant.last_name %> vom Kurs "<%= @course.title %>" abgemeldet und um eine Prüfung einer möglichen Rückerstattung gebeten.

Teilnehmer:        <%= @participant.first_name %> <%= @participant.last_name %>
Kurs:               <%= @course.title %>
Abgemeldet durch:   <%= @cancelled_by || "—" %>
Elternkontakt:      <%= @parent&.email || "—" %>
<% if @course.has_payment? %>
Kursbetrag:         <%= @course.price_display %>
Zahlungsstatus:     <%= @course_registration.payment_cleared? ? "Bezahlt" : "Nicht bezahlt" %>
<% end %>
<% if @reason.present? %>

Begründung:
<%= @reason %>
<% end %>

Bitte prüfe, ob eine Rückerstattung an <%= @parent&.email || "den Elternteil" %> ausgelöst werden soll.

---
Diese E-Mail wurde automatisch vom BTV Anmeldungssystem versendet.
