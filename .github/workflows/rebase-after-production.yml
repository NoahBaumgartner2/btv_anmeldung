name: Rebase after Production Deploy

on:
  push:
    branches: [ main ]

jobs:
  rebase:
    runs-on: ubuntu-latest
    # Nur wenn es ein echter Merge/Push auf main ist (nicht der CI selbst)
    steps:
      - name: Checkout repository (full history)
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Rebase staging onto main
        run: |
          git fetch origin
          git checkout staging
          git rebase origin/main
          git push origin staging --force-with-lease

      - name: Rebase workingbranch onto staging
        run: |
          git checkout workingbranch
          git rebase origin/staging
          git push origin workingbranch --force-with-lease
